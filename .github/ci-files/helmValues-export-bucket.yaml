global:
  clusterId: helm-testing
exportBucket:
  secret:
    config: |-
      type: S3
      config:
        bucket: "kubecost-helm-testing-metrics"
        endpoint: "s3.amazonaws.com"
        region: "us-east-2"
