{{- if (.Values.reporting).logCollection }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "kubecost.aggregator.serviceAccountName" . }}-log-collection
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubecost.commonLabels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "kubecost.aggregator.serviceAccountName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kubecost.aggregator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
---
{{- end }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "kubecost.aggregator.serviceAccountName" . }}
  labels:
    {{- include "kubecost.commonLabels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "kubecost.aggregator.serviceAccountName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kubecost.aggregator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }} 
---
